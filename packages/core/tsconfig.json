{
  "extends": "../tsconfig.default.json",
  "compilerOptions": {
    "target": "esnext",
    "rootDir": "src"
  },
  "include": [
    "./src/**/*" // Only include what is in src (-> dist, tests, .. will be excluded)
  ]
}
